#ifndef __utils__
#define __utils__

#include <string>

char *copyFromString(std::string &str);

void initRand();

int randomInteger(int min, int max);

char *randomWord(int len);

#endif //__utils__
